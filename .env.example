# Configuration du serveur
# L'hôte 0.0.0.0 permet l'accès depuis n'importe quelle interface réseau (multi-plateforme)
AD_WEB_HOST=0.0.0.0
AD_WEB_PORT=5000

# Configuration Flask
SECRET_KEY=changer-ceci-par-une-chaine-aleatoire-securisee
FLASK_ENV=development
FLASK_DEBUG=true

# Configuration Active Directory (valeurs par défaut optionnelles)
AD_SERVER=
AD_PORT=389
AD_USE_SSL=false
AD_BASE_DN=

# Chemins (optionnel - détectés automatiquement selon le système)
# Windows: C:/ProgramData/ADWebInterface/logs
# Linux: /var/log/ad-web-interface
# AD_LOG_DIR=
# AD_DATA_DIR=
